# Default PostgreSQL connection parameters for Docker
DEFAULTS = {
    "PG_HOST": "postgres",  # Docker service name
    "PG_PORT": 5432,
    "PG_DATABASE": "job_db",
    "PG_USER": "airflow",
    "PG_PASSWORD": "airflow"
}
