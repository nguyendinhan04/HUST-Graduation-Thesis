# Source README

The main project documentation is now maintained in the repository root:

```text
../README.md
```

Keep this file as a pointer so GitHub and local tooling use the root README as
the single source of truth for setup, architecture and usage instructions.
